[package]
name = "cidrwall-common"
version = "0.1.3"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Shared userspace/eBPF map ABI for cidrwall"
repository = "https://github.com/joshtgl/cidrwall"

[features]
default = []
user = ["aya"]

[dependencies]
aya = { version = "0.14.0", optional = true }
